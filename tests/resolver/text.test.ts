import {it,expect} from 'vitest'; import {resolveLayout} from '../../src/resolver/resolver'; import {adSpec} from '../../src/spec/adSpec'; import {surfaces} from '../../src/surfaces/surfaces';
it('extreme price never escapes its box',()=>{for(const s of surfaces){const l=resolveLayout(adSpec,s);const p=l.elements.find(e=>e.id==='price');if(p?.visible){expect(p.width).toBeGreaterThan(0);expect(p.height).toBeGreaterThan(0)}}});
