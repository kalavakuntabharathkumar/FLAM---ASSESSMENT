import {describe,it,expect} from 'vitest'; import {resolveLayout} from '../../src/resolver/resolver'; import {adSpec} from '../../src/spec/adSpec'; import {surfaces,unknownSurface} from '../../src/surfaces/surfaces';
describe('geometry',()=>{for(const s of [...surfaces,unknownSurface])it(`${s.name} is valid`,()=>{const l=resolveLayout(adSpec,s);expect(l.validation.valid).toBe(true);for(const e of l.elements.filter(e=>e.visible)){expect(e.x).toBeGreaterThanOrEqual(s.safeArea.left-.01);expect(e.y).toBeGreaterThanOrEqual(s.safeArea.top-.01);expect(e.x+e.width).toBeLessThanOrEqual(s.width-s.safeArea.right+.01);expect(e.y+e.height).toBeLessThanOrEqual(s.height-s.safeArea.bottom+.01)}})});
