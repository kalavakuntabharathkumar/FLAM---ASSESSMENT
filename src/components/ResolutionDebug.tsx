import type { ResolvedLayout } from '../types/layout';
export function ResolutionDebug({layout}:{layout:ResolvedLayout}){return <aside className="debug"><h2>Resolution Debug</h2><div className={layout.validation.valid?'status ok':'status bad'}>{layout.validation.valid?'VALID LAYOUT':'INVALID CANDIDATE'}</div><dl><dt>Composition</dt><dd>{layout.composition}</dd><dt>Surface</dt><dd>{layout.width} × {layout.height}</dd><dt>Visible elements</dt><dd>{layout.elements.filter(e=>e.visible).length}</dd><dt>Score</dt><dd>{layout.score.toFixed(1)}</dd></dl><h3>Elements</h3>{layout.elements.map(e=><div className="debug-row" key={e.id}><b>{e.id}</b><span>{e.visible?'visible':'hidden'} · {Math.round(e.x)},{Math.round(e.y)} · {Math.round(e.width)}×{Math.round(e.height)}</span></div>)}{layout.degradation.length>0&&<><h3>Degradation</h3>{layout.degradation.map((d,i)=><div className="decision" key={i}><b>{d.operation}</b> {d.elementId}<small>{d.reason}</small></div>)}</>}</aside>}
